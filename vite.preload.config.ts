//
// This Vite config exists so Electron Forge can build the preload script
// separately from main/renderer with the correct pipeline entry.
//
import { defineConfig } from 'vite';

// https://vitejs.dev/config
export default defineConfig({});
